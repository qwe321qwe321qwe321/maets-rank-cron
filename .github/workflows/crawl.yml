name: Steam Rank Crawler

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  crawl:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - run: node crawl.js

      - name: Commit rank CSVs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add wishlist_rank.csv top_seller_rank.csv wishlist_rank_growth.csv top_seller_rank_growth.csv
          git diff --cached --quiet || git commit -m "Update rank CSVs $(date -u +'%Y-%m-%d')"
          git push
